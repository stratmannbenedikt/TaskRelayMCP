services:
  taskrelay:
    build: .
    environment:
      TASKRELAY_TOKENS: taskrelay-local-dev:READ_WRITE
    volumes:
      - taskrelay-dev-data:/data
    ports:
      - "127.0.0.1:8080:8080"

volumes:
  taskrelay-dev-data:
